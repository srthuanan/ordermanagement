javascript:(function(){if(typeof Xrm==='undefined'){alert('Vui lòng mở trang DMS VinFast trước khi bấm!');return;}const endpoint="https://jwvgxqrkjlbewvpkvucj.supabase.co/rest/v1/thongtinxe?on_conflict=vin";const apiKey="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imp3dmd4cXJramxiZXd2cGt2dWNqIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3MjUyNTUyNywiZXhwIjoyMDg4MTAxNTI3fQ.R8XaLf9RuB9ICMM3Uti4faIOgN0Beui9pxh-Vy-t4rU";let allRecords=[];const progressDiv=document.createElement('div');progressDiv.style='position:fixed;top:10px;right:10px;background:black;color:white;padding:15px;z-index:999999;border-radius:5px;font-family:sans-serif;font-size:14px;box-shadow:0 4px 6px rgba(0,0,0,0.3);';progressDiv.innerHTML='Đang khởi tạo bộ lọc chi nhánh...';document.body.appendChild(progressDiv);function getLookupName(item,field){if(!item)return"";const f=field.toLowerCase();for(let k in item){if(item.hasOwnProperty(k)){const kl=k.toLowerCase();if(kl.indexOf(f)!==-1&&kl.indexOf('@odata.community.display.v1.formattedvalue')!==-1){return item[k];}}}for(let k in item){if(item.hasOwnProperty(k)){const kl=k.toLowerCase();if(kl.indexOf(f)!==-1&&kl.indexOf('_value')!==-1){return item[k];}}}return item[field]||"";}function fetchPage(query){Xrm.WebApi.retrieveMultipleRecords("xts_inventorynewvehicle",query).then(function success(result){allRecords=allRecords.concat(result.entities);progressDiv.innerHTML='Đã tải được '+allRecords.length+' dòng dữ liệu kho xe...';const nextLink=result["@odata.nextLink"]||result.nextLink;if(nextLink){const nextQuery=nextLink.substring(nextLink.indexOf('?'));fetchPage(nextQuery);}else{progressDiv.innerHTML='Đã tải xong '+allRecords.length+' dòng. Đang xử lý bóc tách xe trùng và đã bán...';processAndSend();}},function(error){progressDiv.remove();alert('Lỗi đọc dữ liệu từ DMS: '+error.message);});}function processAndSend(){const soldVins=new Set();allRecords.forEach(item=>{const orderId=getLookupName(item,"xts_lastvehicleorderid");const vin=String(item.xts_chassisnumber||"").trim().toUpperCase();if(orderId&&vin&&vin.length===17){soldVins.add(vin);}});const mapped=allRecords.filter(item=>{const vin=String(item.xts_chassisnumber||"").trim().toUpperCase();return vin&&vin.length===17&&!soldVins.has(vin);}).map(item=>({vin:String(item.xts_chassisnumber||"").trim().toUpperCase(),so_may:String(item.xts_enginenumber||""),mo_ta:String(item.xts_productdescription||""),khu_vuc:getLookupName(item,"xts_siteid")||"",phien_ban:getLookupName(item,"xts_configurationid")||"",ngoai_that:getLookupName(item,"xts_vehicleexteriorcolorid")||"",noi_that:getLookupName(item,"xts_vehicleinteriorcolorid")||"",nam_san_xuat:item.xts_productionyear?parseInt(item.xts_productionyear):null,inventory_id:"",check_sum:"",so_ton_kho:"",so_tham_chieu:"",ma_san_pham:"",so_don_hang_cuoi:"",modified_timestamp:item.modifiedon?new Date(item.modifiedon).getTime():0}));const uniqueDataMap=new Map();mapped.forEach(item=>{const existing=uniqueDataMap.get(item.vin);if(!existing||item.modified_timestamp>=existing.modified_timestamp){uniqueDataMap.set(item.vin,item);}});const finalData=Array.from(uniqueDataMap.values()).map(({modified_timestamp,...rest})=>rest);if(finalData.length===0){progressDiv.remove();alert('Không có xe nào mới (chưa bán) của chi nhánh để đồng bộ.');return;}let successCount=0;let errorMsg='';function sendChunk(index){if(index>=finalData.length){progressDiv.remove();if(errorMsg){alert('Đã đồng bộ xong, thành công '+successCount+' xe. Một số xe bị lỗi:\n'+errorMsg);}else{alert('Đồng bộ thành công trọn vẹn '+successCount+' xe chưa bán của chi nhánh về hệ thống!');}return;}const chunk=finalData.slice(index,index+200);progressDiv.innerHTML='Đang đồng bộ dữ liệu lên hệ thống ('+index+' / '+finalData.length+')...';fetch(endpoint,{method:'POST',headers:{'apikey':apiKey,'Authorization':'Bearer '+apiKey,'Content-Type':'application/json','Prefer':'resolution=merge-duplicates, return=minimal'},body:JSON.stringify(chunk)}).then(res=>{if(res.ok){successCount+=chunk.length;sendChunk(index+200);}else{res.text().then(errText=>{errorMsg+='Lỗi chunk '+index+'-'+(index+chunk.length)+': '+errText+'\n';sendChunk(index+200);});}}).catch(err=>{errorMsg+='Lỗi kết nối: '+err.message+'\n';sendChunk(index+200);});}sendChunk(0);}let filterQuery="statecode eq 0";try{const userSettings=Xrm.Utility.getGlobalContext().userSettings;if(userSettings&&userSettings.businessUnitId){const buId=userSettings.businessUnitId.replace(/[{}]/g,"").toLowerCase();filterQuery+=" and _xts_businessunitid_value eq '"+buId+"'";}}catch(e){}const selectFields=["xts_chassisnumber","xts_enginenumber","xts_productdescription","xts_siteid","xts_configurationid","xts_vehicleexteriorcolorid","xts_vehicleinteriorcolorid","xts_productionyear","modifiedon","xts_lastvehicleorderid","xts_businessunitid"].join(",");fetchPage("?$select="+selectFields+"&$filter="+filterQuery);})();
