javascript:(function(){if(typeof Xrm==='undefined'){alert('Vui lòng mở trang DMS VinFast trước khi bấm!');return;}const progressDiv=document.createElement('div');progressDiv.style='position:fixed;top:10px;right:10px;background:black;color:white;padding:15px;z-index:999999;border-radius:5px;font-family:sans-serif;font-size:14px;box-shadow:0 4px 6px rgba(0,0,0,0.3);width:400px;max-height:500px;overflow-y:auto;';progressDiv.innerHTML='Đang đọc mẫu đơn hàng để lấy danh sách tên cột...';document.body.appendChild(progressDiv);Xrm.WebApi.retrieveMultipleRecords("xts_newvehiclesalesorder","?$top=1").then(function success(result){if(result.entities.length===0){progressDiv.innerHTML='<div style="color:red">Không tìm thấy đơn hàng nào để đọc mẫu cột!</div>';return;}const keys=Object.keys(result.entities[0]).sort();let html='<h3>Danh sách tên cột tìm thấy ('+keys.length+'):</h3>';html+='<textarea style="width:100%;height:150px;font-family:monospace;font-size:12px;background:#222;color:#0f0;border:1px solid #555;padding:5px;">'+keys.join('\n')+'</textarea>';html+='<div style="margin-top:10px;"><button onclick="this.parentElement.parentElement.remove()" style="background:#555;color:white;border:none;padding:5px 10px;cursor:pointer;border-radius:3px;">Đóng</button></div>';progressDiv.innerHTML=html;},function(error){progressDiv.innerHTML='<div style="color:red">Lỗi đọc cột: '+error.message+'</div>';});})();
