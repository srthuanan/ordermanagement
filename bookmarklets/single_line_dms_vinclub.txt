javascript:(function(){ var _Xrm = window.Xrm || (window.parent && window.parent.Xrm) || (window.top && window.top.Xrm); if (!_Xrm || !_Xrm.WebApi) { alert("❌ Vui lòng mở trang VinFast DMS trước khi bấm!"); return; } const apiKey = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imp3dmd4cXJramxiZXd2cGt2dWNqIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3MjUyNTUyNywiZXhwIjoyMDg4MTAxNTI3fQ.R8XaLf9RuB9ICMM3Uti4faIOgN0Beui9pxh-Vy-t4rU"; const supabaseUrl = "https://jwvgxqrkjlbewvpkvucj.supabase.co/rest/v1/khachhang_vinclub"; var oldControl = document.getElementById("vcSyncControl"); if (oldControl) oldControl.remove(); var bgControl = document.createElement('div'); bgControl.id = "vcSyncControl"; bgControl.style = 'position:fixed;top:10px;right:10px;background:#111116;color:#cbd5e1;padding:18px;z-index:999999;border-radius:12px;font-family:sans-serif;font-size:13px;box-shadow:0 12px 30px rgba(0,0,0,0.5);border:1px solid rgba(16,185,129,0.3);width:320px;display:flex;flex-direction:column;gap:8px;backdrop-filter:blur(10px);'; bgControl.innerHTML = '<div style="display:flex;justify-content:space-between;align-items:center;">' + '<b style="color:#10b981;font-size:14px;display:flex;align-items:center;">⚡ VINCLUB AUTOMATIC SYNCHRONIZER</b>' + '<button id="btnVcClose" style="background:#ef4444;color:white;border:none;padding:2px 6px;border-radius:4px;cursor:pointer;font-size:11px;font-weight:bold;">✕</button>' + '</div>' + '<span id="vcStatus" style="color:#f59e0b;font-weight:500;">📍 Đang chuẩn bị đồng bộ...</span>' + '<div style="font-size:11px;color:#94a3b8;">Trạng thái: <span id="vcProgressInfo">Khởi tạo</span></div>' + '<div style="border-top:1px solid #222;margin:4px 0;"></div>' + '<button id="btnStartVcSync" style="width:100%;background:#107c41;color:white;border:none;padding:12px;cursor:pointer;border-radius:6px;font-weight:bold;font-size:13px;line-height:1.4;box-shadow:0 4px 12px rgba(16,124,65,0.35);">⚡ ĐỒNG BỘ TOÀN BỘ VINCLUB VỀ SUPABASE</button>'; document.body.appendChild(bgControl); document.getElementById("btnVcClose").onclick = function() { bgControl.remove(); }; function updateStatus(text, color) { var el = document.getElementById("vcStatus"); if (el) { if (color) el.style.color = color; el.innerHTML = text; } } function updateInfo(text) { var el = document.getElementById("vcProgressInfo"); if (el) el.innerHTML = text; } var customerMap = new Map(); var accountGuidToCustNumberMap = new Map(); var tierPriority = { "diamond": 4, "kim cương": 4, "platinum": 3, "bạch kim": 3, "gold": 2, "vàng": 2, "member": 1, "thành viên": 1 }; function resolveRealRank(item, rawTier, logStr) { if (logStr) { try { var parsed = typeof logStr === "string" ? JSON.parse(logStr) : logStr; if (parsed) { var d = parsed.data || parsed; if (d.tier && (d.tier.tier_name || d.tier.name || d.tier.tier_code)) { return d.tier.tier_name || d.tier.name || d.tier.tier_code; } if (d.tier_name || d.vclub_tier_name || d.current_tier) { return d.tier_name || d.vclub_tier_name || d.current_tier; } } } catch(e) {} } if (item) { var fmt = item["itv_vinclubtiercode@OData.Community.Display.V1.FormattedValue"] || item["itv_vinclubtiername"] || item["itv_vclubtiercode@OData.Community.Display.V1.FormattedValue"]; if (fmt) return fmt; } if (rawTier !== null && rawTier !== undefined && rawTier !== "") { if (typeof rawTier === "string" && isNaN(rawTier)) return rawTier; var codeMap = { 1: "Member", 2: "Gold", 3: "Platinum", 4: "Diamond", 100000000: "Member", 100000001: "Gold", 100000002: "Platinum", 100000003: "Diamond" }; if (codeMap[rawTier]) return codeMap[rawTier]; return String(rawTier); } return ""; } function addOrUpdateCust(cNum, name, phone, oneId, vUserId, realTier) { if (!cNum) return; cNum = String(cNum).trim(); var existing = customerMap.get(cNum) || { ma_khach_hang: cNum, ten_khach_hang: name || "Khách hàng", so_dien_thoai: phone || "", ma_oneid: oneId || "", vclub_user_id: vUserId || "", hang_vinclub: realTier || "", da_xac_thuc: false, updated_at: new Date().toISOString() }; if (name && (existing.ten_khach_hang === "Khách hàng" || !existing.ten_khach_hang)) existing.ten_khach_hang = name; if (phone && !existing.so_dien_thoai) existing.so_dien_thoai = phone; if (oneId && !existing.ma_oneid) existing.ma_oneid = oneId; if (vUserId && !existing.vclub_user_id) existing.vclub_user_id = vUserId; if (realTier) { var oldP = tierPriority[String(existing.hang_vinclub).toLowerCase()] || 0; var newP = tierPriority[String(realTier).toLowerCase()] || 1; if (newP >= oldP || !existing.hang_vinclub) { existing.hang_vinclub = realTier; } } if (vUserId || (existing.vclub_user_id && existing.vclub_user_id !== "") || oneId || (existing.hang_vinclub && existing.hang_vinclub !== "Member" && existing.hang_vinclub !== "")) { existing.da_xac_thuc = true; } if (existing.da_xac_thuc && !existing.hang_vinclub) { existing.hang_vinclub = "Member"; } customerMap.set(cNum, existing); } function fetchAllRecords(entityName, query, accumulated) { accumulated = accumulated || []; return _Xrm.WebApi.retrieveMultipleRecords(entityName, query).then(function(res) { var items = accumulated.concat(res.entities || []); updateInfo('Nạp ' + entityName + ': ' + items.length + ' dòng...'); var nextLink = res["@odata.nextLink"] || res.nextLink; if (nextLink) { var nextQuery = nextLink.substring(nextLink.indexOf('?')); return fetchAllRecords(entityName, nextQuery, items); } return items; }); } function runSyncProcess() { updateStatus('⏳ Step 1: Nạp Bảng Master Account...', '#f59e0b'); customerMap.clear(); accountGuidToCustNumberMap.clear(); fetchAllRecords("account", "?$select=accountid,accountnumber,name,telephone1,itv_vgcustomerid&$filter=statecode eq 0") .then(function(accounts) { accounts.forEach(function(acc) { if (acc.accountnumber) { if (acc.accountid) accountGuidToCustNumberMap.set(acc.accountid.toLowerCase(), acc.accountnumber); addOrUpdateCust(acc.accountnumber, acc.name, acc.telephone1, acc.itv_vgcustomerid, "", ""); } }); updateStatus('⏳ Step 2: Nạp Bảng Profile & Đơn VSO...', '#f59e0b'); var pProf = fetchAllRecords("itv_customerexternalprofile", "?$select=itv_customerexternalprofileid,itv_vclubuserid,_itv_account_value&$filter=itv_vclubuserid ne null") .then(function(profiles) { profiles.forEach(function(p) { var accGuid = p._itv_account_value ? p._itv_account_value.toLowerCase() : ""; var cNum = accGuid ? accountGuidToCustNumberMap.get(accGuid) : ""; if (cNum) { var vUserId = p.itv_vclubuserid ? String(p.itv_vclubuserid) : ""; addOrUpdateCust(cNum, "", "", "", vUserId, ""); } }); }).catch(function() {}); var pOrder = fetchAllRecords("xts_newvehiclesalesorder", "?$select=xts_newvehiclesalesordernumber,xts_customernumber,itv_vinclubtiercode,itv_vinclubinterfacelog&$expand=xts_potentialcustomerid($select=accountnumber,name,itv_vgcustomerid,telephone1)&$filter=statecode eq 0") .then(function(orders) { orders.forEach(function(ord) { var cNum = ord.xts_customernumber || (ord.xts_potentialcustomerid ? ord.xts_potentialcustomerid.accountnumber : ""); var cName = ord.xts_potentialcustomerid ? ord.xts_potentialcustomerid.name : ""; var phone = ord.xts_potentialcustomerid ? ord.xts_potentialcustomerid.telephone1 : ""; var oneId = ord.xts_potentialcustomerid ? ord.xts_potentialcustomerid.itv_vgcustomerid : ""; var realTier = resolveRealRank(ord, ord.itv_vinclubtiercode, ord.itv_vinclubinterfacelog); var vUserId = ""; if (ord.itv_vinclubinterfacelog) { try { var parsed = JSON.parse(ord.itv_vinclubinterfacelog); if (parsed && parsed.data && parsed.data.vclub_user_id) { vUserId = String(parsed.data.vclub_user_id); } } catch(e) {} } if (cNum) { addOrUpdateCust(cNum, cName, phone, oneId, vUserId, realTier); } }); }).catch(function() {}); return Promise.all([pProf, pOrder]); }).then(function() { var finalData = Array.from(customerMap.values()).filter(function(i){ return i.da_xac_thuc; }); if (finalData.length === 0) { updateStatus('⚠️ Không có KH xác thực nào.', '#ef4444'); return; } updateStatus('⏳ Step 3: Đẩy ' + finalData.length + ' KH lên Supabase...', '#f59e0b'); var chunkSize = 200; var successCount = 0; function sendChunk(index) { if (index >= finalData.length) { updateStatus('✅ ĐÃ ĐỒNG BỘ THÀNH CÔNG ' + successCount + ' KH LÊN SUPABASE!', '#10b981'); updateInfo('Hoàn thành lúc: ' + new Date().toLocaleTimeString()); alert('🎉 ĐÃ ĐỒNG BỘ THÀNH CÔNG ' + successCount + ' KHÁCH HÀNG VINCLUB LÊN SUPABASE!'); return; } var chunk = finalData.slice(index, index + chunkSize); updateInfo('Đang gửi lô [' + (index + 1) + ' - ' + Math.min(index + chunkSize, finalData.length) + ' / ' + finalData.length + ']...'); fetch(supabaseUrl + "?on_conflict=ma_khach_hang", { method: 'POST', headers: { 'apikey': apiKey, 'Authorization': 'Bearer ' + apiKey, 'Content-Type': 'application/json', 'Prefer': 'resolution=merge-duplicates, return=minimal' }, body: JSON.stringify(chunk) }).then(function(res) { if (res.ok) { successCount += chunk.length; sendChunk(index + chunkSize); } else { res.text().then(function(err) { updateStatus('❌ Lỗi gửi Supabase: ' + err, '#ef4444'); }); } }).catch(function(err) { updateStatus('❌ Lỗi kết nối: ' + err.message, '#ef4444'); }); } sendChunk(0); }).catch(function(err) { updateStatus('❌ Lỗi: ' + (err.message || err), '#ef4444'); }); } document.getElementById("btnStartVcSync").onclick = runSyncProcess; runSyncProcess(); })();